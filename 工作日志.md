# 工作日志

## 2026-06-23 当前有效状态

### 结论

项目已经完成 mask 坐标修复后的 5-fold 重训、OOF 阈值搜索、后处理搜索和最终配置评估。

当前唯一有效主线：

- 配置：`configs/h200_maskfix_stage1_strategy_e_folds/h200_maskfix_stage1_strategy_e_fold*.yaml`
- checkpoint：`outputs/h200_maskfix_stage1_strategy_e_fold*/best_postprocess.pt`
- OOF 报告：`outputs/maskfix_oof/maskfix_strategy_e_auto_report.md`
- submission 输出目录：`outputs/maskfix_submissions/`

### Mask 坐标问题与修复

已确认旧实验使用了错误 mask 坐标口径：

- RLE decode 使用了 column-major reshape。
- 非方形图像的文件名宽高解析顺序错误。

已修复：

- `src/uwgi/rle.py`：RLE decode/encode 改为 row-major。
- `src/uwgi/dataset.py`：文件名尺寸解析改为 `width, height`。
- `src/uwgi/train.py`：检测过期 `metadata_folds.csv` 并自动重建。

旧 checkpoint、OOF 指标、threshold/postprocess 搜索、submission 文件全部失效。
旧 checkpoint 已删除；旧 metrics/json/csv/md 输出已移入：

```text
outputs/invalid_pre_maskfix_artifacts/
```

该目录仅保留审计用途，不再作为有效结果使用。

### 当前模型

- 模型：`smp_unetplusplus_efficientnet-b3`
- 输入：2.5D 5-slice
- image size：`384`
- 采样：`all_slices`
- classification head：开启
- loss：Dice + BCE + classification BCE
- checkpoint 选择：postprocess-aware
- folds：5-fold GroupKFold by case

说明：旧的 Stage 1 baseline 没有作为单独 baseline 重训。当前有效结果是 maskfix 后的 Strategy E 5-fold；它仍属于 Stage 1 系列主模型路线，只是用 postprocess-aware checkpoint 选择。

### 5-fold 训练结果

| Fold | Final Epoch | Final Valid Dice | Final Postprocess Dice | Best Valid Dice | Best Postprocess Dice |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0 | 20 | 0.9083108245 | 0.9098112023 | 0.9089543776 | 0.9100769448 |
| 1 | 17 | 0.9240019318 | 0.9235977133 | 0.9244849592 | 0.9244982377 |
| 2 | 12 | 0.9086113892 | 0.9139343123 | 0.9112716196 | 0.9139343123 |
| 3 | 19 | 0.9145178546 | 0.9151149749 | 0.9165152998 | 0.9177813302 |
| 4 | 18 | 0.9082457870 | 0.9116322990 | 0.9096874282 | 0.9122484216 |

### OOF 后处理结果

| Stage | Mean Dice | Positive Dice | Empty FP Rate |
| --- | ---: | ---: | ---: |
| OOF threshold + cls gate | 0.9173107277 | n/a | n/a |
| min_area + z | 0.9185714246 | 0.7642339701 | 0.0175445504 |
| 3D component search / final | 0.9188102194 | 0.7634351159 | 0.0170830851 |

最终后处理参数：

```yaml
mask_thresholds: [0.25, 0.25, 0.25]
cls_thresholds: [0.70, 0.80, 0.20]
min_area: [48, 48, 48]
z_min_run: [2, 3, 1]
min_volume: [512, 512, 0]
keep_largest_component: [false, false, true]
component_connectivity: 1
```

### 已完成的工程收尾

- 旧错误 checkpoint 删除。
- 旧错误输出归档到 `outputs/invalid_pre_maskfix_artifacts/`。
- Streamlit workbench 默认指向 maskfix 5-fold 模型。
- submission pipeline 文档改为 maskfix 版本。
- 重新生成本地 empty sample submission 与 local train-format smoke submission。
- 当前 GPU 训练与 OOF 进程均已结束。

### 后续建议

1. 使用当前 maskfix 结果撰写研究报告。
2. 重新生成可视化样例图：成功案例、失败案例、每器官概率图。
3. 如需做消融，重新跑 maskfix 口径下的 Stage 1 baseline、增强策略或 backbone 对比。
